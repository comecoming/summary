.databases             List names and files of attached databases
.schema ?TABLE?        Show the CREATE statements
.show                  Show the current values for various settings
.tables ?TABLE?        List names of tables
.dump ?TABLE? ...      Dump the database in an SQL text format
.backup ?DB? FILE      Backup DB (default "main") to FILE


CREATE TABLE test (int a, char name);
CREATE TABLE test (int a DEFAULT 0, char name DEFAULT mary);
CREATE TABLE IF NOT EXISTS test (int a);
CREATE TABLE test2 AS SELECT * FROM test;

insert into gals values ("summer", 162, 50, 'A', 90);
insert into gals (name, height, weight, face) values ("silver", 152, 45, 50);

.mode col
.headers on
select * from gals
select * from sqlite_master;

